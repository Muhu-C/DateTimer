<Page x:Class="DateTimer.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DateTimer"
      xmlns:ui="https://handyorg.github.io/handycontrol"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="HomePage">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <TextBlock Foreground="{Binding MyVariable}" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Text="主页" FontWeight="Bold" FontSize="24"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Margin="10,10,10,10" Grid.Column="0">
                <TextBlock Foreground="{Binding MyVariable}" FontSize="20" Text="当前时间" FontFamily="FangSong" FontWeight="Bold"/>
                <TextBlock Foreground="{Binding MyVariable}" x:Name="TimeText" FontSize="18" Margin="10,0,0,0" Text="2024/01/01 10:08:35" FontFamily="Consolas"/>
                <TextBlock Foreground="{Binding MyVariable}" FontSize="20" Text="目标时间" FontFamily="FangSong" FontWeight="Bold"/>
                <TextBlock Foreground="{Binding MyVariable}" x:Name="TargetText" FontSize="18" Margin="10,0,0,0" Text="2025/01/01" FontFamily="Consolas"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Foreground="{Binding MyVariable}" Margin="0,0,10,0" FontSize="20" Text="公告" FontFamily="FangSong" FontWeight="Bold"/>
                    <TextBlock Foreground="{Binding MyVariable}" VerticalAlignment="Center" FontSize="14" Text="公告源: " FontFamily="Microsoft YaHei Light"/>
                    <TextBlock Foreground="{Binding MyVariable}" VerticalAlignment="Center" Name="LinkAdd" FontSize="14" Text="Raw Gitee COM" FontFamily="Microsoft YaHei Light"/>
                </StackPanel>
            <TextBlock TextWrapping="Wrap" Foreground="{Binding MyVariable}" MaxWidth="400" x:Name="Notice" FontSize="18" Margin="10,4,0,0" Text="获取中..." FontFamily="FangSong"/>
            </StackPanel>
            <StackPanel Margin="0,10,10,10" Grid.Column="1">
                <Button Style="{StaticResource ButtonSuccess}" FontSize="16" Name="ShowTimeTable" Content="显示时间表" HorizontalAlignment="Center" Width="200" Height="50" Click="ShowTimeTable_Click"/>
                <Button Style="{StaticResource ButtonWarning}" FontSize="16" Name="HideTimeTable" Margin="0,10,0,0" Content="隐藏时间表" HorizontalAlignment="Center" Width="200" Height="50" Click="HideTimeTable_Click"/>
                <Button FontSize="16" Name="GoToSetting" Margin="0,130,0,0" HorizontalAlignment="Center" Width="200" Height="50" Click="GoToSetting_Click">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="设置"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
